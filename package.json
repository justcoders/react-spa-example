{
  "name": "jc-react-spa-example",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://justcoders.github.io/react-spa-example",
  "devDependencies": {
    "gh-pages": "0.12.0",
    "react-scripts": "0.9.1"
  },
  "dependencies": {
    "react": "15.4.2",
    "react-dom": "15.4.2"
  },
  "scripts": {
    "start": "REACT_APP_APIHOST=localhost:3000 react-scripts start",
    "build": "REACT_APP_APIHOST=https://jsonplaceholder.typicode.com react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
